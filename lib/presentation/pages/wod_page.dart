import 'package:flutter/material.dart';

class WodPage extends StatelessWidget {
  const WodPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Wod Page')),
    );
  }
}
